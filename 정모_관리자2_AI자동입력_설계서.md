# 🤖 정모 관리자 2페이지 — AI 자동 입력 설계서

> 작성: 2026-09-11 · 대상: 다음 작업 세션 (Claude Code) + 운영자
> 목표: 리더보드·스코어카드 입력을 **사진 업로드 → 실행 → 저장** 3단계로 줄인다.
> 원칙: **지금 정모 관리자 페이지(admin/event.html)는 그대로 둔다.** 새 페이지를 따로 만든다.

---

## ✅ 구현 완료 (2026-09-15) — 이 문서는 설계 기록으로 보존

실제 구현은 아래와 같이 **설계서와 달라진 부분**이 있다. 최신 사양은 `CLAUDE.md`와
`GokaLeague_System_Specification.md`(Ver 1.4 §4.5)를 기준으로 본다.

| 설계서 | 실제 구현 |
|---|---|
| 스코어카드에서 코스 판독 | **판독하지 않음** — 코스는 대회 세팅(명단) 값 사용 |
| 조각 겹침 160px | **300px** (조각 경계의 잘린 줄이 "판독 못 함"으로 잘못 보고되던 문제) |
| 사진 최대 6장 | 조각 기준 **10장**, 스코어카드는 사진 1장당 호출 1번(동시 3장) |
| (없음) | **판독 결과 JSON 편집칸** — 예전 JSON 붙여넣기처럼 직접 수정 후 [다시 채우기] |
| (없음) | **앱 표시 이름 기억** — 스마트스코어 닉네임을 한 번 맞추면 다음부터 자동 매칭 |
| Preview에서 실행만, 정모 후 병합 | 사용자 결정으로 **정모 당일 메인 사용** → 정모 전 main 병합 |
| 일반대회는 대상 외 | **일반대회 관리자 3단계에도 동일 판독 적용** (2026-09-15) |

**측정 결과:** 스코어카드 504/504칸, 리더보드 28/28명 정답 · 카드 1장 ≈ $0.04, 리더보드 1회 ≈ $0.08
**운영 결과:** 제1회 고카 정모(2026-09-13) 현장 사용 완료

---

## 0. 한눈에 보기

| | 지금 (admin/event.html) | 새 페이지 (admin/event2.html) |
|---|---|---|
| 1 | 프롬프트 복사 | **📷 사진 선택** |
| 2 | AI 앱 열고 붙여넣기 + 사진 업로드 | **🤖 실행** (서버가 AI 호출) |
| 3 | 결과 JSON 복사 | (자동으로 입력표에 채워짐 → 눈으로 확인) |
| 4 | 관리자 페이지에 붙여넣기 → 채우기 | — |
| 5 | 저장 | **💾 저장** (기존과 동일한 저장 함수) |

- 저장 전 **확인표는 그대로 남긴다.** AI가 잘못 읽을 수 있으니, 사람이 보고 저장 버튼을 누른다. **자동 저장은 하지 않는다.**
- JSON 붙여넣기 방식은 **비상용(접힘)** 으로 남긴다. 현장에서 API가 안 되면 바로 전환한다.

---

## 1. 배경과 제약

### 1-1. 일정 리스크
- 정모 2026-09-13. 기존 페이지는 2026-09-11 최종 리허설로 전 과정이 검증됐다.
- 그래서 **아래 파일은 수정 금지**:
  `event.html` · `index.html` · `admin/index.html` · `admin/event.html` · `admin/auth.js`

### 1-2. 보안 사고의 교훈 (2026-09-11)
- service_role 키가 공개 저장소와 공개 관리자 페이지에 노출됐다 → 로그인 방식으로 전환하고 옛 키를 폐기했다.
- **정적 파일(HTML/JS)은 누구나 받아볼 수 있다.** AI API 키도 절대 넣으면 안 된다.
- → AI 호출은 **Vercel 서버리스 함수**에서만 하고, 키는 **Vercel 환경변수**에만 둔다.

### 1-3. 이전 시도 이력
- 2026-08-30에 `api/extract-score.js` + `package.json`을 만들었다가, 당시 사용자 결정(수동 JSON 방식)으로 **삭제**했다.
- 2026-09-11 사용자가 자동화를 원해 **관리자 2페이지로 재도입**한다. (그때 코드는 git 기록에 없다 — 커밋 전에 삭제됨. 새로 작성한다.)

---

## 2. 전체 구조

```
[관리자 휴대폰 · admin/event2.html]
   │ ① 사진 선택 → 브라우저에서 압축 (긴 변 1600px, JPEG 0.8, 최대 6장)
   │ ② 🤖 실행
   ▼
GokaAuth.authFetch('/api/extract-score', { method:'POST', write:true, body })
   │   → Authorization: Bearer <관리자 로그인 토큰>  (auth.js가 자동으로 붙임, 401이면 갱신 후 재시도)
   ▼
[Vercel 함수 · api/extract-score.js]
   │ a. 토큰 검증: Supabase /auth/v1/user  → app_metadata.role === 'admin' 확인
   │    (실패 시 AI를 부르기 전에 거절 → 남이 크레딧을 못 씀)
   │ b. Claude API 호출 (키 = process.env.ANTHROPIC_API_KEY)
   │ c. 구조화된 JSON 반환 + 경고 목록
   ▼
[admin/event2.html]
   │ ③ 기존 입력표(①탭) / 저장 대기 목록(②탭)에 채움 + 검증 경고 표시
   │ ④ 사람이 확인 → 💾 저장 (기존 saveLeaderboard / savePending 그대로 → RLS 권한으로 DB 기록)
```

---

## 3. 파일 목록

| 파일 | 구분 | 내용 |
|---|---|---|
| `admin/event2.html` | **신규** | `admin/event.html` 복사본 + ①②탭에 "📷 AI 입력" 카드 추가. 제목 "정모 관리자 2 (AI)" |
| `api/extract-score.js` | **신규** | Vercel 서버리스 함수. 토큰 검증 → Claude 호출 → JSON 반환 |
| `package.json` | **신규** | `@anthropic-ai/sdk` 의존성. ⚠️ 추가하면 Vercel 빌드에 `npm install` 단계가 생긴다 (11장 참고) |
| 나머지 | **무수정** | event.html · index.html · admin/index.html · admin/event.html · admin/auth.js |

- 진입: 주소 직접 입력 `/admin/event2.html`. 기존 페이지에는 링크를 넣지 않는다(정모 이후 결정).
- 스크립트 경로는 **절대경로**로 쓴다: `<script src="/admin/auth.js">` (예전에 `/admin` 슬래시 문제로 링크가 틀어진 적 있음).

---

## 4. 관리자 화면 흐름

### ① 리더보드 탭
1. **📷 리더보드 사진 선택** — 스마트스코어 리더보드 캡처. 여러 장 가능 (스크롤 캡처)
2. **🤖 실행** — 진행 표시("AI가 읽는 중… 20~40초")
3. 결과가 **기존 36명 오버파 입력표**(`#grid1`)에 채워짐 → 바뀐 칸 노란 테두리(기존 `dirty` 스타일)
4. 경고 표시: 명단에서 못 찾은 이름, 판독 못 한 선수, 진행홀이 이전보다 줄어든 선수(오래된 캡처 의심)
5. **💾 오버파 저장** — 기존 `saveLeaderboard()` 그대로

### ② 스코어카드 탭
1. **📷 스코어카드 사진 선택** — 조별 카드(보통 1장 = 4명). 여러 조를 한 번에 올려도 됨
2. **🤖 실행**
3. 결과가 **기존 저장 대기 목록**(`pending` → `renderPending()`)에 쌓임
4. 경고 표시 (8장 참고): 18홀 합 ≠ 카드에 인쇄된 합계, 한 장의 선수들이 같은 조가 아님, 이미 저장된 값과 다름
5. **💾 홀 스코어 저장** — 기존 `savePending()` 그대로

### 재사용할 기존 함수 (admin/event.html에 이미 있음)
`loadAll` · `matchRecord`(성명→공백무시→닉네임 매칭) · `applyLeaderboard(players)` · `applyScorecard(players)` · `renderPending` · `saveLeaderboard` · `savePending` · `parsOf` / `parsFull` / `basePar` · `parseJsonArray` · `toOver` · 프롬프트 원본 `LEADER_PROMPT` / `SCORE_PROMPT`

→ AI 결과를 `applyLeaderboard` / `applyScorecard`가 받는 형식(`[{name, over}]`, `[{name, scores[18]}]`)으로 넘기면, **채우기·매칭·저장 로직은 새로 만들 필요가 없다.** 새로 만드는 건 "사진 → 서버 → 결과" 부분과 검증 경고뿐이다.

---

## 5. API 계약 — `POST /api/extract-score`

### 요청
```http
POST /api/extract-score
Authorization: Bearer <Supabase 관리자 access token>   ← GokaAuth.authFetch가 자동으로 붙임
Content-Type: application/json
```
```json
{
  "mode": "leaderboard" | "scorecard",
  "images": ["data:image/jpeg;base64,...", "..."],        // 1~6장
  "roster": [{"name":"김성만","nick":"사랑은성","group":"벨리5조","front":"벨리","back":"파크"}],
  "pars":   {"파크":[4,5,4,3,4,4,3,4,5], "벨리":[5,4,4,3,4,5,4,3,4]}   // scorecard일 때
}
```

### 응답 200
```json
// leaderboard
{ "mode":"leaderboard", "players":[{"name":"김성만","over":5,"holes":12}], "warnings":[], "usage":{"input_tokens":0,"output_tokens":0} }
// scorecard
{ "mode":"scorecard", "players":[{"name":"김성만","scores":[0,2,-1, ...18개],"printed_total":84}], "warnings":[], "usage":{...} }
```

### 오류 코드 규칙 ⚠️ 중요
| 코드 | 의미 | 비고 |
|---|---|---|
| **401** | **Supabase 로그인 토큰이 없거나 무효** | **이 경우에만** 401. auth.js가 갱신·재로그인 처리 |
| 403 | 로그인은 됐지만 관리자 아님 | |
| 400 | 요청 형식 오류 (mode, images 등) | |
| 413 | 이미지가 너무 크거나 많음 | |
| 429 | AI 호출 한도 | |
| 502 | AI가 판독 실패 / 거절(refusal) / 형식 불일치 | |
| 503 | `ANTHROPIC_API_KEY` 미설정 | 안내 문구 포함 |
| 500 | 기타 | |

> **Anthropic 쪽 인증 오류(키 틀림 등)를 401로 내보내면 안 된다.** `authFetch`가 401을 "로그인 만료"로 보고 재로그인을 반복한다. → 502/503으로 매핑한다.

---

## 6. 서버 함수 설계 (`api/extract-score.js`)

1. **메서드 검사** — POST만.
2. **토큰 검증** — `GET {SUPABASE_URL}/auth/v1/user`
   헤더 `apikey: <publishable 키>`, `Authorization: Bearer <받은 토큰>`
   → 200이 아니면 401, `user.app_metadata.role !== 'admin'`이면 403.
   (publishable 키만으로 충분. **secret / service_role 키는 쓰지 않는다.**)
3. **입력 검증** — mode, 이미지 1~6장, `data:image/(jpeg|png|webp|gif);base64,` 형식.
4. **Claude 호출**
   - 공식 SDK `@anthropic-ai/sdk`, 모델 **`claude-opus-5`**, 이미지 base64 블록 + 텍스트 프롬프트.
   - **구조화 출력(json_schema)** 으로 형식 강제 (7장 스키마).
   - `stop_reason`(거절 등) 처리, 서버측 fallback 사용 여부는 **claude-api 스킬 기준**으로 결정.
   - ⚠️ SDK 사용법은 추측하지 말고 **작업 세션에서 claude-api 스킬을 먼저 불러 최신 방식**으로 작성.
   - effort(추론 강도): 정확도가 최우선. 테스트 사진으로 정확도·응답시간을 재 보고 정한다.
5. **실행시간** — AI 호출이 20~40초 걸릴 수 있다. Vercel 함수 기본 제한시간에 걸리지 않도록 **현재 요금제의 최대 실행시간을 확인하고 설정**한다(작업 세션에서 확인).
6. **본문 크기** — Vercel 요청 본문 한도(약 4.5MB) → 브라우저에서 압축(긴 변 1600px, JPEG 0.8), 최대 6장.
7. **로그 금지** — 이미지·토큰·키를 로그에 남기지 않는다. 응답에는 토큰 사용량(usage)만 넣는다.

---

## 7. 프롬프트 설계 (서버에 둔다)

- 출발점: `admin/event.html`의 `LEADER_PROMPT`, `SCORE_PROMPT` (리허설에서 검증된 규칙).
- **명단을 함께 준다** — 성명·닉네임·조. 명백히 같은 사람이면 **명단 표기(성명)** 로 통일.
- **불확실하면 빼라, 추측하지 마라** — 빈 칸을 지어내면 더 위험하다. 빠진 선수는 경고로 알린다.

### 리더보드
- `over`: 오버파 누계. `E`·`이븐`=0, 언더는 음수, `+7`→7.
- `holes`: 진행홀(없으면 null). 여러 장에 같은 선수가 있으면 **진행홀이 많은 쪽**.

### 스코어카드
- 선수당 **정확히 18개**, 1번→18번 **플레이 순서**.
- 파=0, 버디=-1, 이글=-2, 보기=1, 더블=2 …
- 카드가 **실제 타수(4,5,3…)** 로 적혀 있으면 par를 빼서 변환 → 선수별 전반/후반 코스와 `pars`를 프롬프트에 넣어 준다.
  (파크조 = 전반 파크·후반 벨리, 벨리조 = 전반 벨리·후반 파크)
- **카드에 인쇄된 선수 합계(`printed_total`)도 같이 읽는다** → 교차검증용 (없으면 null).

### 출력 스키마 (구조화 출력)
- leaderboard: `{ players: [{ name: string, over: integer, holes: integer|null }] }`
- scorecard: `{ players: [{ name: string, scores: integer[18], printed_total: integer|null }] }`
- 모든 객체 `additionalProperties: false`, 필드 전부 `required`.

---

## 8. 정확도 안전장치 (브라우저 쪽 검증)

| 검사 | 대상 | 처리 |
|---|---|---|
| 이름 매칭 실패 | 공통 | 경고 + 기존 선수 선택 드롭다운 |
| 18개가 아님 | 스코어카드 | 목록에서 제외 + 경고 (기존 로직) |
| **18홀 합 ≠ 인쇄 합계** | 스코어카드 | ⚠️ 빨간 경고. 타수 합계 = 기준타 + 오버파 합으로 비교 |
| **한 장의 선수들이 같은 조가 아님** | 스코어카드 | 경고 (다른 조 카드 섞임 의심) |
| 이미 저장된 값과 다름 | 스코어카드 | 표시 (덮어쓰기 주의) |
| **진행홀이 이전보다 줄어듦** | 리더보드 | 경고 (오래된 캡처 의심) |
| 채워진 인원 | 리더보드 | "36명 중 N명" 표시 |

- 경고가 있어도 저장은 막지 않는다(현장 판단). 단, 경고는 눈에 띄게.
- **자동 저장 금지.** 저장은 항상 사람이 버튼을 누른다.

---

## 9. 보안 체크리스트

- [ ] `ANTHROPIC_API_KEY`는 **Vercel 환경변수에만** (Production + Preview). 코드·채팅·커밋 금지.
- [ ] 작업 세션은 **키 값을 사용자에게 요구하지 않는다.** 등록 여부는 함수가 503을 내는지로 확인.
- [ ] 함수는 **관리자 토큰 검증 전에는 AI를 호출하지 않는다.**
- [ ] Anthropic 콘솔에서 **월 사용 한도(spend limit)** 설정 → 사고가 나도 피해 상한이 있다.
- [ ] secret / service_role 키는 서버 함수에도 쓰지 않는다 (토큰 검증은 publishable 키로 충분).
- [ ] 이미지·토큰·키를 로그에 남기지 않는다.
- [ ] 커밋 전 `git grep`으로 `sk-ant-`, `sb_secret_`, `eyJhbGci`(JWT) 문자열이 없는지 확인.

---

## 10. 비용 추정 (추정치 — 실측으로 확인)

- 모델 `claude-opus-5`: 입력 $5 / 출력 $25 (100만 토큰당).
- 1회 호출: 사진 1~2장 + 프롬프트 ≈ 입력 4~6천 토큰, 출력(추론 포함) ≈ 2~4천 토큰
  → **1회 약 $0.05~0.12 (약 70~170원)**.
- 정모 하루: 리더보드 10회 + 스코어카드 9회 ≈ 20회 → **약 $1~2.5 (1,500~3,500원)**.
- 응답의 `usage`로 실제 토큰을 확인해서 이 추정을 갱신한다.

---

## 11. 배포 전략 (정모 직전 리스크 관리)

- `package.json`을 추가하면 Vercel이 `npm install` 단계를 거친다 → **지금의 순수 정적 배포 방식이 바뀐다.**
- 그래서:
  1. **브랜치 `feature/ai-score`** 에서 작업한다.
  2. Vercel **Preview 배포 URL**에서 검증한다. (Preview는 Vercel 로그인 보호가 걸려 있을 수 있음)
  3. **main 병합은 사용자가 승인할 때만.** 정모(9/13) 전이면 정모 이후 병합을 기본으로 한다.
- **정모 당일 권장 운영**
  - 저장은 **검증된 기존 페이지(admin/event.html)** 로 한다.
  - 2페이지(Preview)는 **"실행"만** 해서 추출 결과를 기존 방식 결과와 비교 → 실전 정확도 데이터를 모은다.
  - ⚠️ **두 페이지에서 동시에 저장하지 않는다** (같은 DB).

---

## 12. 테스트 계획

### 12-1. 정답이 있는 사진으로 정확도 측정 ⭐
- **과거 대회 스코어카드 캡처**를 쓴다 → 그 대회의 `tb_Record` H1~H18이 **정답**이다.
- AI 결과를 DB 값과 **자동 대조**해서 홀 단위 정확도(%)와 틀린 홀 목록을 낸다.
- 리더보드도 과거 대회 캡처 ↔ 기록 합계로 대조.
- ⚠️ 2페이지는 정모(20260913) 전용이다. 과거 대회 사진은 **함수만 직접 호출**해서 결과를 대조한다(저장하지 않음).

### 12-2. 오류 경로
토큰 없음 → 401 / 비관리자 → 403 / 키 미설정 → 503 / 너무 큰 이미지 → 413 / 흐린 사진 → 빠진 선수 경고 / 다른 조 카드 섞임 → 경고.

### 12-3. 회귀 없음 확인
- `git diff --stat`에 **무수정 파일 5개가 없어야 한다.**
- 저장 경로는 기존 함수 그대로 — 드라이런(쓰기 가로채기)으로 저장 페이로드 확인.
- 리허설 원본 데이터: `_backup/2026-09-11/tb_Record.json`의 정모 36명 스코어 (N홀 리더보드 = H1~HN 합).

---

## 13. 사용자가 준비할 것

1. **Anthropic API 키** — console.anthropic.com → API Keys에서 발급 → 크레딧 충전 → **월 사용 한도 설정**.
   (Claude 구독과는 별개인 종량제)
2. **Vercel 환경변수 등록** — Vercel → 프로젝트 → Settings → Environment Variables
   `ANTHROPIC_API_KEY` = 발급한 키 (Production, Preview 체크). **값은 채팅에 붙이지 않는다.**
3. **테스트 사진**
   - 스마트스코어 **리더보드 캡처** 2~3장
   - **스코어카드 캡처** 몇 장 — 가능하면 **이미 DB에 기록된 과거 대회**(정답 대조 가능)
   - 프로젝트 폴더에 `_testphotos/` 같은 폴더를 만들어 넣어 두면 좋다 (git 제외 처리)

---

## 14. 완료 기준

- [ ] 관리자가 **사진 선택 → 실행 → 저장** 3단계로 리더보드·스코어카드를 입력할 수 있다.
- [ ] 정답 있는 사진으로 정확도를 측정해 보고했다 (홀 단위 %).
- [ ] 8장 경고가 모두 동작한다.
- [ ] 5장 오류 코드가 규칙대로 나오고, 로그인 만료 시 재로그인이 정상이다 (무한 반복 없음).
- [ ] 9장 보안 체크리스트 전부 통과.
- [ ] 무수정 파일 5개는 diff에 없다.
- [ ] Preview에서 검증 완료. main 병합은 사용자 승인 후.

---

## 15. 새 세션 시작 프롬프트 (복사해서 사용)

```
정모 관리자 2페이지(AI 자동 입력) 작업을 시작합니다.

먼저 프로젝트 루트의 `정모_관리자2_AI자동입력_설계서.md`와 `CLAUDE.md`를 읽고,
설계서대로 진행해 주세요.

핵심 요구
- 목표: 리더보드·스코어카드 입력을 "사진 선택 → 실행 → 저장" 3단계로 줄이기.
  저장 전 확인표는 유지하고, 자동 저장은 하지 않습니다.
- 기존 파일(event.html, index.html, admin/index.html, admin/event.html, admin/auth.js)은
  절대 수정하지 마세요. 새 파일은 admin/event2.html, api/extract-score.js, package.json 입니다.
- AI 호출은 Vercel 서버리스 함수에서만 합니다. ANTHROPIC_API_KEY는 Vercel 환경변수에만 있고,
  코드·커밋에 넣지 않습니다. 저에게 키 값을 요구하지 마세요.
- 함수는 Supabase 관리자 로그인 토큰(app_metadata.role = 'admin')을 검증한 뒤에만 AI를 호출합니다.
  401은 로그인 토큰 문제에만 쓰세요 (설계서 5장).
- Claude API 코드는 claude-api 스킬을 먼저 불러서 최신 사용법으로 작성하세요.
  (모델 claude-opus-5, 구조화 출력)
- 작업은 브랜치 feature/ai-score 에서 하고 Vercel Preview로 검증합니다.
  main 병합은 제가 승인할 때만 합니다.
- 기존 JSON 붙여넣기는 비상용으로 남겨 두세요.

진행 방식
1. 설계서를 읽고 궁금한 점이나 바꿔야 할 점이 있으면 먼저 알려 주세요.
2. 12장 테스트 계획대로, 정답이 있는 과거 대회 사진으로 정확도를 측정해서 보고해 주세요.
3. 단계마다 무엇을 했고 무엇을 확인했는지 알려 주세요.

테스트 사진 위치: (여기에 폴더 경로를 적어 주세요)
```
