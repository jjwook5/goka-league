# ⛳ 고카리그 (GOKA LEAGUE) 시스템 개발 명세서 Ver 1.4

본 문서는 **고카리그(GOKA LEAGUE) Ver 1.3** 의 아키텍처, 데이터베이스 설계, 비즈니스 로직 및 UI/UX 구조를 기록한 공식 명세서입니다.  
향후 기능 고도화 및 유지보수 시 본 문서를 기준으로 처리합니다.

---

## 1. 시스템 개요 (System Overview)

| 항목 | 내용 |
|------|------|
| **시스템명** | 고카리그 (GOKA LEAGUE) Ver 1.3 |
| **대상** | 반기별 승강제 골프 동호회 운영 관리자 및 회원 |
| **배포 환경** | GitHub + Vercel (정적 호스팅, 자동 배포) |
| **데이터베이스** | Supabase (PostgreSQL) |
| **백엔드 미들웨어** | **없음** — 프론트엔드가 Supabase REST API 직접 호출 |
| **회원 접속 URL** | `golfriend.vercel.app` |
| **관리자 접속 URL** | `golfriend.vercel.app/admin/` |

### 1.1. 주요 핵심 기능

1. **시즌제 승강제 연동** — 상/하반기 단위 독립 시즌 운영 및 1부/2부/3부 리그 관리
2. **AI 스코어 추출 연동** — 스코어카드 이미지를 AI가 분석하여 JSON 변환 → 붙여넣기 기반 입력
3. **신페리오 정산 엔진** — 무작위 제외홀 추첨 + 소수 둘째자리 정밀 핸디캡 연산 (스마트스코어 기준)
4. **운세 분석 (Luck Factor)** — 추첨 운에 의한 변수를 실력 점수와 비교 계량화
5. **관리자 통합 관리** — 회원/코스/시즌/대회 CRUD를 관리자 화면에서 직접 처리
6. **모바일 퍼스트 PWA** — 홈 화면 설치 시 네이티브 앱처럼 동작 (주소창 없음)

---

## 2. 시스템 아키텍처

### 2.1. 현재 아키텍처 구조도

```
[사용자 스마트폰]                        [관리자 스마트폰]
       │                                        │
       ▼                                        ▼
golfriend.vercel.app              golfriend.vercel.app/admin/
  (index.html)                       (admin/index.html)
       │   SUPABASE_ANON_KEY              │   SUPABASE_SERVICE_ROLE_KEY
       │                                  │
       └──────────────┬───────────────────┘
                      ▼
          Supabase REST API
          https://ogyzzmlxxmplwaawraoc.supabase.co
                      │
                      ▼
          PostgreSQL Database (Supabase)
          ┌──────────────────────────┐
          │  tb_member               │
          │  tb_Course               │
          │  tb_Match_Episodes       │
          │  tb_Record               │
          │  tb_Season               │
          └──────────────────────────┘
```

### 2.2. 구성 요소 역할

| 구성 요소 | 역할 |
|-----------|------|
| **GitHub** | 프론트엔드 코드 원본 저장소 (jjwook5/goka-league) |
| **Vercel** | GitHub 코드 인터넷 호스팅 + 자동 배포 (커밋 후 ~30초) |
| **Supabase** | PostgreSQL DB 호스팅 + REST API 제공 |
| **Google Apps Script (Code.gs)** | **폐기** — 더 이상 사용하지 않음 |
| **Vercel 서버리스 함수** (`api/extract-score.js`) | 스코어카드·리더보드 **사진 AI 판독** (Ver 1.4 신설). 관리자 로그인 토큰 검증 후에만 AI 호출 |
| **Anthropic Claude API** | 사진 → JSON 판독 (모델 `claude-opus-5`, 구조화 출력) |

### 2.3. 인증 · API 키 관리 (Ver 1.4 개편)

| 키 / 토큰 | 사용처 | 비고 |
|---------|--------|------|
| Supabase **publishable 키** | 모든 화면 (`apikey` 헤더) | 공개되어도 무방. 권한은 RLS가 판단 |
| Supabase **로그인 토큰(JWT)** | 관리자 화면 (`Authorization: Bearer`) | `admin/auth.js`가 발급·갱신. `app_metadata.role='admin'`만 쓰기 가능 |
| `ANTHROPIC_API_KEY` | `api/extract-score.js` (서버에서만) | **Vercel 환경변수(Production+Preview)에만 저장.** 코드·커밋 금지 |
| ~~`SUPABASE_SERVICE_ROLE_KEY`~~ | **폐기** | 2026-09-11 유출 사고로 제거·교체. 어떤 파일에도 두지 않는다 |

**쓰기 권한 구조:** RLS 정책 `goka_admin_all` + 함수 `is_goka_admin()` → 로그인 토큰의 `app_metadata.role = 'admin'` 인 계정만 INSERT/UPDATE/DELETE 가능.
**관리자 계정 추가:** 대시보드 Authentication → Users → Add user(Auto Confirm) → SQL로 `raw_app_meta_data`에 `{"role":"admin"}` 부여.
**로그인 유지:** 접속 토큰 1시간 + 갱신 토큰(기본 만료 없음). 로그아웃은 **모든 기기**를 로그아웃시킨다.

> ⚠️ 정적 파일(HTML/JS)은 누구나 내려받을 수 있다. **비공개 키는 어떤 형태로도 프론트엔드에 두지 않는다.**

### 2.4. Supabase REST API 호출 패턴

```javascript
// 공통 헤더 설정
const SB_H  = { 'apikey': KEY, 'Authorization': 'Bearer ' + KEY };
const SB_HW = { ...SB_H, 'Content-Type': 'application/json', 'Prefer': 'return=minimal' };

// 조회 (GET)
fetch(`${SUPABASE_URL}/rest/v1/{테이블}?select=*{필터}`, { headers: SB_H })

// 삽입 (POST)
fetch(`${SUPABASE_URL}/rest/v1/{테이블}`, { method:'POST', headers: SB_HW, body: JSON.stringify(rows) })

// 수정 (PATCH)
fetch(`${SUPABASE_URL}/rest/v1/{테이블}?{필터}`, { method:'PATCH', headers: SB_HW, body: JSON.stringify(fields) })

// 삭제 (DELETE)
fetch(`${SUPABASE_URL}/rest/v1/{테이블}?{필터}`, { method:'DELETE', headers: SB_H })

// 한국어 필터값은 반드시 encodeURIComponent() 적용
// 예: `전화번호=eq.${encodeURIComponent('010-1000-0001')}`
```

---

## 3. 데이터베이스 설계 (Supabase PostgreSQL)

### 3.1. `tb_member` — 회원 마스터

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `순번` | int | 자동 증가 |
| `전화번호` | text | **PK** — 실제로는 회원번호로 사용 |
| `성명` | text | 실명 |
| `닉네임` | text | 선택 입력 |
| `소속리그` | text | 1부 / 2부 / 3부 / **NULL(게스트)** |
| `등급` | text | **일반 / 스텝 / 게스트** |
| `활동상태` | text | **활동중** / 중지 (소프트 삭제) |

> **등급 정책:** `일반`(정회원) / `스텝`(운영진, 용도 미정·분류만) / `게스트`(비회원 임시 참가자).
> 게스트는 `소속리그 = NULL`로 등록되며 리그순위·대회결과에서 제외된다 (§4.6 참조).

**회원번호 채번 규칙** (필드명은 `전화번호` 유지)

| 구분 | 형식 | 예시 |
|------|------|------|
| 남성 | `010-1000-XXXX` | `010-1000-0001` |
| 여성 | `010-2000-XXXX` | `010-2000-0002` |
| **게스트** | `010-9000-XXXX` | `010-9000-0001` |

- 일반 회원: 뒷 4자리(XXXX)는 **1000/2000 번호대 공통 최댓값 + 1** (게스트 9000대 제외)
- 게스트: **9000 번호대 내 최댓값 + 1**로 독립 채번 (일반 회원 번호 연속성 보존)
- 관리자 화면에서 성별/등급 선택 시 자동 채번 표시 (수동 입력 불가)

---

### 3.2. `tb_Course` — 코스 마스터

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `구장명` | text | **PK(복합)** |
| `코스명` | text | **PK(복합)** |
| `P1`~`P9` | int | 홀별 기준타수 (3 / 4 / 5) |
| `사용여부` | text | **Y** / N (소프트 삭제) |

- `사용여부 = 'Y'` 인 코스만 대회 매핑 화면 및 신규 대회 생성 시 표시
- 코스 삭제 시 물리 삭제 없이 `사용여부 = 'N'`으로 처리하여 과거 기록 무결성 유지

---

### 3.3. `tb_Match_Episodes` — 대회 에피소드

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `대회번호` | bigint | `YYYYMMDD` 형식 날짜 채번 |
| `대회명` | text | 대회 이름 |
| `날짜` | date | 라운딩 날짜 |
| `구장명` | text | 사용 골프장 |
| `코스` | text | 9홀 코스명 (전반/후반 각각 1행) |
| `제외홀` | text | 신페리오 추첨 결과 (쉼표 구분, 예: `2,5,7`) |
| `완료구분` | text | **진행중** / 마감 / 완료 (3단계) |

- 하나의 대회에 코스 수만큼 행이 생성 (일반적으로 구장 내 코스 수 = 2~3행)
- **완료구분 3단계 상태 머신:**

| 단계 | 관리자 세션 드롭다운 | 사용자 대회 콤보박스 | 신페리오/번외/청백전 |
|------|------|------|------|
| **진행중** | 표시됨 | **미표시** | - |
| **마감** | 표시됨 `[마감]` | 표시됨 | 🔒 잠금 |
| **완료** | **미표시** | 표시됨 | 전체 공개 |

---

### 3.4. `tb_Record` — 성적 트랜잭션 (핵심)

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `순번` | int | 자동 증가 |
| `대회번호` | bigint | **PK(복합)** |
| `전화번호` | text | **PK(복합)** — tb_member FK |
| `구장명` | text | |
| `전반코스` | text | 전반 9홀 코스명 |
| `후반코스` | text | 후반 9홀 코스명 |
| `성명` | text | 대회 시점 성명 스냅샷 ※표시는 tb_member 최신값 우선 |
| `닉네임` | text | 대회 시점 닉네임 스냅샷 ※표시는 tb_member 최신값 우선 |
| `조번호` | text | 예: 1조, 2조 |
| `소속팀` | text | 백팀 / 청팀 (대회마다 재배정) |
| `소속리그` | text | 대회 시점 리그 스냅샷 |
| `H1`~`H18` | int | **오버파 표기법** 홀별 스코어 |
| `합계` | numeric | 그로스 스코어 (기준타 + 오버파 합산) |
| `NP_Handicap` | numeric | 신페리오 핸디캡 (소수 둘째자리) |
| `Net_Score` | numeric | 그로스 − NP_Handicap (소수 둘째자리) |
| `Luck_Factor` | numeric | 운세 지수 (소수 둘째자리) |

> ⚠️ **성명/닉네임 표시 정책**: 화면 렌더링 시 `tb_member`의 최신값을 우선 사용.  
> 회원이 닉네임을 변경해도 리그순위·대회결과에 최신 닉네임이 자동 반영됨.  
> `tb_Record`의 스냅샷 필드는 과거 기록 참조 목적으로만 유지.

**스코어 오버파 표기법**

| 결과 | DB 저장값 |
|------|----------|
| 이글 | `-2` |
| 버디 | `-1` |
| 파 | `0` |
| 보기 | `1` |
| 더블보기 | `2` |
| 트리플 이상 | `3` 이상 |

---

### 3.5. `tb_Season` — 시즌 마스터

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `시즌ID` | text | PK, 예: `S2026_1` |
| `시즌명` | text | 예: `2026년 상반기` |
| `시작_대회번호` | bigint | 시즌 첫 대회번호 (YYYYMMDD) |
| `종료_대회번호` | bigint | 시즌 마지막 대회번호 / **NULL = 진행중** |
| `진행상태` | text | **진행중** / 종료 |

- 시즌 범위 필터: `대회번호 >= 시작_대회번호 AND 대회번호 <= 종료_대회번호`
- 진행중 시즌은 `종료_대회번호 IS NULL` → 별도 처리 필요

---

## 4. 핵심 비즈니스 로직

### 4.1. 신페리오 제외홀 추첨

- **실행 시점:** 관리자 화면 → 대회관리 → 3단계 스코어 입력 페이지의 **🎲 제외홀 추첨 & 신페리오 정산** 버튼
- **전제 조건 (Ver 1.4 변경):** 모든 선수의 **홀 스코어(H1) 입력** + **전반/후반 코스 지정**이 끝나야 실행.
  둘 중 하나라도 빠지면 해당 선수 이름과 함께 안내 후 중단한다.
  (예전 기준 `합계 ≥ 1`은 폐기 — 합계는 정산이 다시 계산해 채우므로 비어 있어도 진행 가능)
- **알고리즘:** `tb_Course` 마스터에서 코스별 P3/P4/P5 홀 번호 분류 후, 파별 각 1개 무작위 추출 (9홀당 3개 = 18홀 총 6개 제외홀)
- **저장:** `tb_Match_Episodes.제외홀`에 쉼표 구분 문자열로 저장
- **재추첨 가능:** 대회 완료 전까지 버튼을 여러 번 눌러 재추첨 가능
- **🎲 버튼 표시:** 스코어 임시저장 후에도 항상 표시 (별도 영역으로 분리)

### 4.2. 신페리오 핸디캡 (NP Engine)

모든 계산 로직은 **브라우저 측 JavaScript**에서 실행됩니다 (서버 미들웨어 없음).  
**스마트스코어 앱 기준** — 버디 이하 실제 음수값 그대로 사용 (언더파 캡처리 없음).

```
① 제외홀 적용
   선택된 12개 홀의 오버파 실제값 합산 (버디=-1, 파=0, 보기=1 등 그대로)
   overParSelected = 추첨된 12개 홀의 실제 오버파 합산

② NP_Handicap 공식
   NP_Handicap = Math.round(overParSelected × 1.5 × 0.8 × 100) / 100
                 → 소수 둘째자리

③ 그로스 스코어
   합계 = 코스 기준타수 합산 + 18홀 오버파 실제값 합산
   ※ 버디(-1), 이글(-2) 등 언더파는 보정 없이 실제 음수 그대로 합산
   ※ 임시저장 시 합계 자동 계산하여 저장

④ Net_Score
   Net_Score = Math.round((합계 - NP_Handicap) × 100) / 100
               → 소수 둘째자리

⑤ Luck_Factor
   Luck_Factor = Math.round((NP_Handicap - (합계 - 기준타) × 0.8) × 100) / 100
                 → 소수 둘째자리
                 양수: 행운아 (핸디캡을 많이 받음)
                 음수: 불운왕 (핸디캡을 적게 받음)
```

> **Ver 1.1 대비 수정사항:**  
> - 버디 캡처리(`Math.max(0, score)`) 제거 — 실제 음수값 그대로 사용  
> - Luck_Factor 공식: `overParTotal(capped) × 0.8` → `(합계 - 기준타) × 0.8`

### 4.3. 동점자 처리 규정

순위는 저장된 필드 없이 **렌더링 시점에 JavaScript로 실시간 계산**됩니다.  
모든 화면(관리자·사용자)에 동일한 기준이 적용됩니다.

#### 신페리오 순위

| 순서 | 기준 | 방향 |
|------|------|------|
| 1차 | Net_Score | 낮은 순 |
| 2차 | 합산(Gross) | **높은 순** ← 재미 요소 (같은 Net에서 더 많이 친 사람 우선) |
| 3차 | 백카운트 | 후반9홀 → 후반8홀 → ... → H18 단독 (낮은 순) |

#### 스트로크 순위

| 순서 | 기준 | 방향 |
|------|------|------|
| 1차 | 합산(Gross) | 낮은 순 |
| 2차 | 백카운트 | 후반9홀 → 후반8홀 → ... → H18 단독 (낮은 순) |

#### 백카운트 알고리즘

```
후반9홀 합계(H10~H18) 비교
  └ 같으면 → 후반8홀(H11~H18) 비교
       └ 같으면 → 후반7홀(H12~H18) 비교
            └ ...
                 └ 같으면 → H18 단독 비교
                      └ 완전 동일 → 공동순위 부여
```

#### 정모(팀 대항전) 전용 규칙 — Ver 1.4

**팀 합산 동점** (`cmpTeamBestFirst`, 2026-09-15 변경)

| 순서 | 기준 |
|------|------|
| 1차 | 팀 합산 그로스 낮은 순 |
| 2차 | 각 팀 **1위(가장 잘 친) 선수**끼리 비교 → 낮은 팀이 앞 |
| 3차 | 2위끼리 → 3위끼리 … 순으로 내려가며 비교 |

> 이전 규칙(최하위 선수부터 비교)은 폐기. 동점으로 갈린 팀에는 "동점→1위부터 비교" 표시.

**신페 수상자 선정** (`npAwardWinners`, 2026-09-15 신설)

- 스트로크와 신페는 **따로 시상**하므로, **메달리스트(스트로크 1위)는 신페 우승 대상에서 제외**한다.
- 신페 순위표의 **순위 자체는 바꾸지 않고**, 실제 수상자에게만 **👑**을 표시한다.
- 팀 보너스 +10도 실제 수상자의 팀으로 간다.
- 적용 파일: `event.html`, `admin/event.html`, `admin/event2.html` (**정모 전용** — 일반대회는 신페 우승 시상 자체가 없다).

**정모 팀 점수 구성**

| 항목 | 점수 |
|------|------|
| 팀 합산 스트로크 순위 | 1등 100 / 2등 90 / 3등 80 / 4등 70 |
| 메달리스트 | +10 |
| 신페 남·여 우승 | 각 +10 (메달리스트 제외 규칙 적용) |
| 니어리스트 | +10 (수동 입력) |
| 버디 1개당 | +5 (이글은 버디로 세지 않음) |
| 신페 꼴등 | −10 |

### 4.4. 리그순위 정렬 기준

| 순서 | 기준 | 방향 |
|------|------|------|
| 1차 | 평균 그로스 타수 | 낮은 순 |
| 2차 | 시즌 참가횟수 | 많은 순 |
| 3차 | 최근 대회 성적 | 낮은 순 (가장 최근 대회의 합계) |

> 시즌 내 대회를 날짜(대회번호) 오름차순 정렬 후 집계하여 "최근 대회"를 정확히 판별.

### 4.5. AI 사진 판독 (Ver 1.4 신설)

사진을 관리자 화면에서 바로 판독한다. 별도 AI 앱을 열 필요가 없다.

```
[관리자 화면] 사진 선택 → 브라우저에서 압축·조각내기 (가로 1080px, JPEG)
   → POST /api/extract-score  (Authorization: 관리자 로그인 토큰)
   → [서버] 토큰·관리자 권한 검증 → Claude(claude-opus-5) 구조화 출력 호출
   → 결과 JSON → 화면 입력표·검증표에 채움 → 사람이 확인 → 💾 저장(기존 저장 함수)
```

**적용 화면**

| 화면 | 모드 | 결과 형식 |
|------|------|-----------|
| 정모 관리자 2 ①리더보드 | `leaderboard` | `[{name, over, holes}]` |
| 정모 관리자 2 ②스코어카드 | `scorecard` | `[{name, scores[18]}]` |
| 일반대회 관리자 3단계 | `scorecard` | 〃 |

**원칙**
- **자동 저장하지 않는다.** 저장은 항상 사람이 버튼을 누른다.
- **판독 결과 JSON 편집칸**을 제공한다. 이름 오타·숫자 오판독을 JSON에서 고친 뒤 [다시 채우기]. 줄을 복사해 빠진 선수를 추가할 수 있다.
- 사진 1장당 호출 1번, 동시 3장까지 병렬 처리.
- 리더보드 긴 스크롤 캡처는 **겹침 300px로 세로 조각내기**(한 줄이 잘려 중복 보고되지 않도록).
- **앱 표시 이름 기억(정모):** 스마트스코어 닉네임이 명단과 다를 때 한 번 맞춰 주면 그 기기에 기억(localStorage `goka_ev2_alias`)하고, 다음 판독부터 AI 명단에 함께 넘겨 자동 매칭한다.

**검증 경고**

| 경고 | 판정 |
|------|------|
| ⛔ 카드 인쇄 합계(9홀 T열)와 입력 합계 불일치 | 오판독 가능성 — 사진과 대조 |
| ⛔ 18홀이 아님 | 저장에서 제외 |
| ⚠️ 명단에 없는 이름 | 선수 선택칸 또는 JSON에서 수정 |
| ⚠️ 같은 사진에 다른 조 선수 섞임 / 조에서 빠진 선수 | 카드 누락·혼입 확인 |
| ⚠️ 이미 저장된 값과 다름 | 덮어쓰기 주의 |
| ⚠️ 진행홀이 이전보다 줄어듦(그날 기준) | 오래된 캡처 의심 |

**오류 코드 규칙** — `401`은 **로그인 토큰 문제에만** 쓴다(auth.js가 재로그인 처리). `403` 관리자 아님 / `400` 형식 오류 / `413` 용량 초과 / `429` AI 한도 / `502` AI 실패·거절 / `503` `ANTHROPIC_API_KEY` 미설정.

**비용·정확도(실측 2026-09-11):** 스코어카드 1장 ≈ $0.04·10초, 리더보드 1회 ≈ $0.08·10초. 정답 대조 결과 스코어카드 504/504칸, 리더보드 28/28명 일치. 정모 하루 약 $1.5.

> **비상 대비:** 각 화면에 기존 **JSON 붙여넣기 / 표준 프롬프트 복사**를 접어서 유지한다. AI 호출이 막히면 예전 방식 그대로 입력할 수 있다.

### 4.6. 게스트 처리 규정

대회에 참가하는 **비회원 임시 참가자(게스트)**를 위한 분기 규칙. (Ver 1.3 신설)

**식별:** `등급 = '게스트'` + `소속리그 = NULL` + 회원번호 `010-9000-XXXX`

**노출 정책 (B안 — 청백전 전용):**

| 화면 | 게스트 | 근거 |
|------|--------|------|
| 리그순위 | ❌ **영구 제외** | 등급=게스트 명시 제외 + 소속리그 NULL 자동 탈락 (이중 안전) |
| 신페리오 | ❌ 제외 | 정식 시상 대상 아님 |
| 스트로크 | ❌ 제외 | 〃 |
| 번외타이틀 | ❌ 제외 | 〃 |
| **청백전** | ✅ **포함** | 팀 전력으로 반영 (소속팀 청/백 기준) |
| 통계 요약(인원·평균) | ❌ 제외 | 정식 참가자 기준 |

**구현 메모:**
- `tb_Record`에 별도 컬럼을 추가하지 않음. 순위는 렌더링 시점 실시간 계산이므로,
  게스트 포함본(`allRecs`)/제외본(`recs`)을 분리해 탭별로 골라 쓴다.
- 게스트 여부는 `getMemberInfo(전화번호).등급 === '게스트'`로 판별 (회원/관리자 동일).
- "게스트를 특정 대회결과에만 노출"하는 대회별 가변 정책은 채택하지 않음
  (필요 시 `tb_Record`에 노출 플래그 컬럼 추가하는 A안으로 확장 가능).

---

## 5. 관리자 화면 워크플로우

### 5.1. 메인 탭 구조 (4개)

| 탭 | 주요 기능 |
|----|-----------|
| ⚙️ 대회관리 | 대회 생성·편집, 조편성, 코스매핑, 스코어입력, 신페정산, 결과조회 |
| 👤 회원관리 | 신규 회원 등록, 정보 수정, 활동상태 전환(활동중↔중지) |
| ⛳ 코스관리 | 신규 코스 등록, 사용여부 전환(Y↔N) |
| 🏆 시즌관리 | 시즌 생성, 종료 처리 |

### 5.2. 대회관리 단계 탭 (4단계)

```
1단계: 세팅&수정  →  2단계: 현장 매핑  →  3단계: 스코어 입력  →  4단계: 결과조회
```

#### 1단계 — 세팅&수정

- 대회명, 날짜, 구장, 참가 인원 설정
- **대기명단:** 활동중 회원을 **닉네임 오름차순**으로 정렬 표시 (닉네임 없으면 성명 기준)
  - 칩 표시 형식: `닉네임 (성명) 소속리그`
- 대기명단에서 선택 → 조 편성 (4명 × N조), 조 표시는 성명 사용
- 팀 배정 (백팀/청팀) 토글
- `[명단 확정 및 DB 적재]` → `tb_Record` 선수 행 사전 삽입 + `tb_Match_Episodes` 생성

#### 2단계 — 현장 매핑

- 조별 전반/후반 코스 배정 (사용여부=Y 코스만 표시)
- **진입할 때마다 DB에서 최신 코스를 다시 읽어** 드롭다운에 적용 (Ver 1.4 — 캐시로 인한 오표시 제거)
- 선택칸 맨 위에 **[— 선택 —]** 항목. 상단에 `✅ DB에 저장된 코스: N / M조` 표시
- 모든 조를 고르지 않으면 **저장 차단** (빈 코스로 저장되면 3단계에서 합계가 계산되지 않음)
- `[코스 매핑 저장]` → `tb_Record.전반코스` / `후반코스` 업데이트 + 화면 캐시 갱신

#### 3단계 — 스코어 입력

1. **📷 스코어카드 사진 선택 → 🤖 AI 실행** (Ver 1.4) — 사진마다 판독 결과·빠진 선수 안내
2. 판독 결과가 **JSON 칸에 채워지고 검증표가 자동 생성**됨 → 틀린 곳은 JSON에서 고친 뒤 **🔍 검증 표 만들기**
   - 검증표: 카드 합계 불일치 ⛔, 명단에 없는 이름 ⚠️, 18홀 아님, 저장된 값과 차이, 저장 가능 인원 요약
   - 비상용으로 **📋 표준 프롬프트 복사 + JSON 붙여넣기**를 접어서 유지
3. **💾 임시저장** → `tb_Record.H1~H18` + **합계(그로스) 자동 계산** 업데이트
   - 이름 매칭: 성명 → 공백무시 → 닉네임. 못 찾은 선수·18홀 아닌 선수는 **제외 목록으로 안내**하고 실제 저장 인원을 알려 준다
   - **전반/후반 코스가 없으면 저장 차단** (합계를 계산할 수 없어 정산이 막히므로 2단계로 안내)
4. **🎲 제외홀 추첨 & 신페리오 정산** — 항상 표시 (임시저장 후에도 유지)
   - 홀 스코어 미입력 또는 코스 미지정 선수가 있으면 이름과 함께 안내 후 중단 (§4.1)
   - 정산에서 건너뛴 선수가 있으면 결과 알림에 이름과 원인을 표시

#### 4단계 — 결과조회

**상단 — 마감 버튼**
- **🚩 마감** → `tb_Match_Episodes.완료구분 = '마감'` 저장
  - 사용자 화면 대회 콤보박스에 노출 시작
  - 신페리오/번외타이틀/청백전은 잠금(🔒) 상태 (스트로크만 공개)

**중단 — 결과 서브탭 (3개)**

| 서브탭 | 내용 |
|--------|------|
| 신페리오 | Net_Score 기준 순위 (동점자 처리 규정 적용), 금/은/동 색상 강조 |
| 번외타이틀 | 버디/파/보기/더블 집계, **헤더 클릭 시 해당 기준 정렬** + 강조 표시 |
| 청백전 | 청팀/백팀 대결 구도, 팀 합계, 승리팀 👑 표시 |

**하단 — 결과공개 버튼**
- **🏆 결과공개** → `tb_Match_Episodes.완료구분 = '완료'` 저장
  - 모든 탭(신페리오/번외타이틀/청백전) 사용자 공개
  - 관리자 세션 드롭다운에서 제외

### 5.3. 세션 이어하기 엔진

- 화면 최상단 `[작업할 대회 선택]` 드롭다운 상시 노출
- `완료구분 != '완료'`인 진행중 대회만 목록에 표시 (`[마감]` 레이블 병기)
- 중간에 브라우저가 종료되어도 드롭다운에서 선택 시 조편성 데이터 복원 후 작업 재개 가능
- **대회를 고르면 그 대회 기록을 DB에서 새로 읽는다** (Ver 1.4) — 다른 기기·다른 세션에서 바꾼 조편성·코스가 즉시 반영됨

### 5.4. 회원 등록 자동채번

1. 성별 선택 (남성 / 여성)
2. 회원번호 자동 표시 (읽기전용)
   - 기존 전체 회원의 뒷 4자리 최댓값 + 1로 채번
3. 성명, 닉네임, 소속리그, 등급 입력 후 등록

---

## 6. 사용자 앱 UI/UX 구성

### 6.1. 🏆 리그순위 탭

- 시즌 드롭다운 (과거 시즌 아카이브 조회)
- 1부/2부/3부 리그 전환 탭
- **닉네임 + 성명 병기 표시** (tb_member 최신값 기준)
- 정렬 기준: 평균타수 → 참가횟수 많은 순 → 최근대회 성적 (§4.4 참고)
- 승격권(상위 2명) 좌측 파란 테두리, 강등권(하위 2명) 빨간 테두리
- 선수 행 터치 → 선수카드 탭 이동

### 6.2. 📋 대회결과 탭

- 대회 선택 콤보박스: **마감** 또는 **완료** 상태 대회만 표시 (진행중 제외)
- 대회 선택 시 참가인원/평균타수/총버디/총파 통계 요약
- **🎯 신페리오 제외홀 표시** (Ver 1.4) — 요약 아래에 코스별 제외홀을 `4번(파3) · 6번(파4) · 9번(파5)` 형식으로 노출.
  완료(전체 공개) 대회만 표시하며, 추첨 전 대회는 줄 자체가 나오지 않는다.

**탭별 공개 정책 (완료구분 기준)**

| 탭 | 마감 | 완료 |
|----|------|------|
| 스트로크 | ✅ 공개 | ✅ 공개 |
| 신페리오 | 🔒 잠금 | ✅ 공개 |
| 번외타이틀 | 🔒 잠금 | ✅ 공개 |
| 청백전 | 🔒 잠금 | ✅ 공개 |

- 🔒 잠금 상태: "집계 중 / 시상 후 결과가 공개됩니다." 안내 화면 표시

**신페리오 탭**
- **순위(#) 컬럼** 표시 — 1/2/3위 금/은/동 색상 강조
- 동점자 처리 규정 적용 (§4.3 참고)
- 합계·Net·Handicap·Luck 소수 2자리 표시

**스트로크 탭**
- 그로스 기준 순위, 동점자 백카운트 처리

**번외타이틀 탭**
- 버디/파/보기/더블 통합 테이블
- **컬럼 헤더 클릭 시 해당 기준 재정렬 + 강조 표시**

**청백팀 결과**
- 좌우 분할, 팀 합계, 승리팀 👑 표시

### 6.3. 👤 선수카드 탭

**목록 화면**
- **닉네임순 / 성명순 토글 버튼** (우측 상단)
- 선택 기준 오름차순 정렬
- 닉네임순 표시: `닉네임 (성명)` / 성명순 표시: `성명 (닉네임)`
- 기본 모드: 닉네임순

**프로필 화면 (선수 선택 후)**
- 통산 출전/평균/베스트/최근 스코어 대시보드
- 그로스 추이 SVG 꺾은선 그래프 (평균선 포함)
- 대회별 홀별 스코어 기록 (오버파 표기법, 컬러 스킨)

**홀별 기록 레이아웃 (대회당 1블록)**

```
[구장명] · [코스명]                   ← 코스 헤더 (회색 소문자)
•       —                          ← 파 기호 행 (파3=•파란, 파4=공백, 파5=—주황)
0 +1 -1  +1 +2  0 +1  0 +1  OUT 82(+10)  ← 오버파 스코어 + 소계
```

**파 기호 표기 규칙**

| 파 종류 | 기호 | 색상 |
|---------|------|------|
| 파3 | `•` (불릿) | 파란색 (`#60a5fa`) |
| 파4 | (공백) | — |
| 파5 | `—` (대시) | 주황색 (`#fb923c`) |

### 6.4. PWA 앱 설치

| 구분 | 매니페스트 | 앱 이름 |
|------|-----------|---------|
| 회원용 | `manifest.json` | 고카리그 |
| 관리자용 | `manifest-admin.json` | 고카관리 |

- 설치: iOS Safari "공유 → 홈 화면에 추가" / Android Chrome "홈 화면에 추가"
- 설치 후 주소창 없는 standalone 전체 화면 동작

---

## 7. 파일 구조

```
goka-league/                         ← GitHub 저장소 루트
├── index.html                       ← 회원용 앱 (리그순위·대회결과·선수카드)
├── event.html                       ← 회원용 정모 페이지 (스코어집계·최종 종합·개인시상)
├── admin/
│   ├── index.html                   ← 일반대회 관리자 앱 (4단계)
│   ├── event2.html                  ← 정모 관리자 2 (AI 사진 판독) — 현재 메인
│   ├── event.html                   ← 정모 관리자 1 (수동 JSON) — 대체용
│   └── auth.js                      ← 관리자 로그인 공용 모듈 (Supabase Auth)
├── api/
│   └── extract-score.js             ← 사진 AI 판독 서버리스 함수 (Claude API)
├── package.json                     ← @anthropic-ai/sdk 의존성 (함수 빌드용)
├── manifest.json                    ← 회원용 PWA 설정
├── manifest-admin.json              ← 관리자용 PWA 설정
├── .claude/
│   ├── settings.json                ← Claude Code 프로젝트 설정
│   └── launch.json                  ← 로컬 미리보기 서버 설정 (npx serve, port 3344)
├── Code.js                          ← 구 GAS 코드 참조용 보관 (미사용)
├── CLAUDE.md                        ← 개발 브리핑
└── GokaLeague_System_Specification.md  ← 본 문서
```

### 7.1. 핵심 파일별 역할 요약

| 파일 | 역할 | 주요 전역 변수 |
|------|------|----------------|
| `index.html` | 회원용 SPA | `DATA`, `currentTab`, `currentLeague`, `currentSeasonId`, `currentMatchId`, `currentResultSubTab`, `currentTitleSort`, `currentPlayer`, `profileNameMode` |
| `event.html` | 회원용 정모 SPA | `RECORDS`, `NEAREST`, `STATUS`, `AWARD_SUB`, `TITLE_SORT` |
| `admin/index.html` | 일반대회 관리자 SPA | `globalData`, `activeMatches`, `assignedGroups`, `currentMatchId`, `currentVenue`, `isEditMode`, `phase4SubTab`, `phase4TitleSort`, `aiFileList`, `aiLast` |
| `admin/event2.html` | 정모 관리자 2 (AI) | `MEMBERS`, `RECORDS`, `EPISODES`, `COURSES`, `VENUE`, `STATUS`, `pending`, `aiFiles`, `aiLast` |
| `admin/event.html` | 정모 관리자 1 (수동 JSON) | 〃 (AI 관련 변수 없음) |
| `admin/auth.js` | 로그인·토큰 갱신·`authFetch` | `GokaAuth` |
| `api/extract-score.js` | 사진 판독 API (Node, Vercel) | `maxDuration 300`, 모드 `leaderboard`/`scorecard` |

---

## 8. 운영 및 유지보수 지침

### 8.1. 코드 수정 반영 프로세스

1. `C:\Users\51\Naver MYBOX\07. Utility App\01. goka-league\` 폴더에서 파일 수정
2. Git 커밋 & 푸시 → Vercel 자동 재배포 (약 30초)

```bash
git add index.html event.html admin/index.html admin/event.html admin/event2.html admin/auth.js api/extract-score.js
git commit -m "수정 내용 설명"
git push origin main
```

**권장 절차 (Ver 1.4)**

1. 기능 수정은 **브랜치**에서 진행 → Vercel **Preview**에서 검증 → 사용자 승인 후 `main` 병합
2. 검증은 **DB 쓰기를 가로챈 드라이런**으로 한다 (운영 DB 오염 방지)
3. 병합이 끝난 브랜치는 원격·로컬 모두 삭제해 `main` 하나만 유지
4. `package.json`이 있어 Vercel 빌드에 `npm install` 단계가 포함된다 (정적 파일 배포는 그대로)
5. `ANTHROPIC_API_KEY`를 새로 등록·변경하면 **재배포해야 적용**된다

### 8.2. Supabase DB 직접 수정

- Supabase 대시보드 → Table Editor 또는 SQL Editor에서 직접 수정
- 컬럼 추가/변경 시 프론트엔드 코드도 함께 수정 필요

### 8.3. 로컬 미리보기 서버

```bash
# Claude Code 미리보기 (launch.json 기반)
# 또는 직접 실행:
npx serve -l 3344 .
# → http://localhost:3344/index.html (회원용)
# → http://localhost:3344/admin/index.html (관리자용)
```

### 8.4. 시즌 교체 절차 (관리자 화면에서 처리)

1. **시즌관리 탭** → 현재 진행중 시즌 → 종료 날짜 선택 → **[시즌 종료]**
2. **시즌관리 탭** → **[+ 새 시즌 생성]** → 시즌ID/시즌명/시작날짜 입력
3. **회원관리 탭** → 승강제 대상 회원의 소속리그 수동 변경

### 8.5. 신규 코스 추가 절차

1. **코스관리 탭** → **[+ 신규 코스 등록]**
2. 구장명, 코스명, 홀별 파(P1~P9) 입력 후 등록
3. 즉시 대회 매핑 화면에 반영됨

### 8.6. 향후 개선 고려 사항

| 항목 | 내용 |
|------|------|
| 스코어 직접 입력 | JSON 변환 없이 홀별 터치 입력 UI |
| 대회 결과 PDF 출력 | 정산 결과 공유용 출력 기능 |
| 푸시 알림 | 대회 일정, 결과 알림 |
| 순위 고정 필드 | 결과 공개 시점에 신페리오/스트로크 순위를 tb_Record에 저장 |

---

## 9. 버전 이력 (Changelog)

### Ver 1.4 — 정모 팀 대항전 + 사진 AI 판독 (2026-09-15)

**보안 — service_role 키 폐기 (2026-09-11)**
- 공개 저장소·관리자 페이지에서 `SERVICE_ROLE_KEY` 유출 → 키 교체 후 **Supabase 로그인 방식**으로 전환
- 공개 키는 legacy anon JWT → **publishable 키**로 교체, 쓰기 권한은 RLS(`is_goka_admin()`)가 판단
- 관리자 로그인 공용 모듈 `admin/auth.js` 신설 (토큰 자동 갱신, 401 시 재로그인)

**정모(팀 대항전) 기능**
- 회원용 `event.html` — 스코어집계 / 최종 종합 / 개인시상 3탭 (탭 이름 [라운드 중] → **[스코어집계]**)
- 정모 관리자 `admin/event.html`(수동) → **`admin/event2.html`(AI)** 로 주 사용 전환. 관리자 헤더 [🏆 정모 관리]가 event2로 연결
- 팀 점수 = 순위점수 + 메달리스트·신페 남/여 우승·니어 + 버디×5 − 신페 꼴등 (§4.3)
- **팀 동점 규칙:** 각 팀 1위부터 비교 (이전 최하위부터 비교 규칙 폐기)
- **스트로크 우승자는 신페 우승에서 제외** — 순위는 유지하고 실제 수상자에게 👑 (§4.3)

**사진 AI 판독 (§4.5)**
- `api/extract-score.js` 신설 — 관리자 토큰 검증 후 Claude(`claude-opus-5`) 구조화 출력 호출
- 정모 리더보드·스코어카드, 일반대회 3단계 스코어 입력에 적용
- 판독 결과 JSON 편집칸, 검증표 경고, 앱 표시 이름 기억, 긴 캡처 조각내기(겹침 300px)
- 실측 정확도: 스코어카드 504/504칸, 리더보드 28/28명

**관리자 화면 안정화 (2026-09-15)**
- 조편성 덮어쓰기 시 **조별 전반/후반 코스 유지** (예전에는 빈값으로 초기화되어 정산이 막혔음)
- 2단계 [— 선택 —] 추가·미선택 저장 차단, 저장된 코스 수 표시
- 대회 선택·2단계·제외홀 확인·번외타이틀 정렬을 **DB 최신값 기준**으로 변경 (캐시 의존 제거)
- 임시저장: 코스 없으면 차단, 이름 매칭 확대(성명→공백무시→닉네임), 제외 선수·실제 저장 인원 안내
- 정산 전 점검 기준을 `합계 ≥ 1` → **홀 스코어 유무**로 변경, 건너뛴 선수 보고

**회원 화면**
- 대회결과 요약 아래 **🎯 신페리오 제외홀** 표시 (코스별 홀 번호 + 파)

### Ver 1.3 — 게스트 참가자 기능 (2026-06-21)

**게스트 등급 신설 (§3.1, §4.6)**
- 등급 선택지 통일: `일반 / 스텝 / 게스트` (신규·수정 폼 일치, 기존 `선수`/`매니저` 폐기)
- 게스트 전용 회원번호대 `010-9000-XXXX` 독립 채번 (일반 1000/2000대와 분리)
- 게스트는 `소속리그 = NULL`로 등록

**게스트 노출 분기 (청백전 전용)**
- 리그순위·신페리오·스트로크·번외·통계 요약에서 게스트 제외
- 청백전에만 포함 (팀 전력으로 반영)
- `tb_Record` 컬럼 추가 없이, 렌더링 시 포함본/제외본 분리로 구현
- 회원용·관리자 양쪽 동일 적용

**부수 정비**
- 동점자 처리(스트로크·번외타이틀) 동순위 부여 로직 보강
- 백카운트 알고리즘 정정: 누적합 → H18부터 홀별 역순 단독 비교
- Supabase numeric 타입 문자열 비교 버그 수정 (`Number()` 강제 변환)
- 방문 기록(MAU 측정) 기능 추가: localStorage UUID + `visits` 테이블 (하루 1회)

### Ver 1.2 — 로직 고도화 및 UX 개선 (2026-06-01)

**신페리오 NP 계산 공식 수정 (스마트스코어 기준 적용)**
- 버디/이글 언더파 캡처리(`Math.max(0, score)`) 제거 → 실제 음수값 그대로 사용
- Luck_Factor 공식: `overParTotal(capped) × 0.8` → `(합계 - 기준타) × 0.8`
- 임시저장 시 합계(그로스) 자동 계산 저장 추가

**동점자 처리 규정 신설 (관리자/사용자 화면 통일)**
- 신페리오: Net_Score → 합산 높은 순(재미 요소) → 백카운트
- 스트로크: 합산 낮은 순 → 백카운트
- 리그순위: 평균타수 → 참가횟수 → 최근대회 성적
- 백카운트: 후반9홀 합계 → 8홀 → ... → H18 단독 (정통 방식)

**성명/닉네임 최신화 정책 도입**
- `getMemberInfo()` 헬퍼 함수 신설
- 리그순위·대회결과 전 탭에서 항상 tb_member 최신값 우선 사용
- 닉네임 변경 시 모든 화면 자동 반영

**관리자 화면 UX 개선**
- 세팅&수정 대기명단: 닉네임 오름차순 정렬, `닉네임(성명)` 형식 표시
- 현장매핑: 대회 선택 시 기저장된 전반/후반 코스 자동 복원
- 번외타이틀: 버디/파/보기/더블 헤더 클릭 정렬 + 현재 기준 강조
- 🎲 버튼 항상 표시 영역 분리 (임시저장 후에도 유지)
- 신페리오 정산: 전원 합계 입력 완료 검증 추가

**사용자 화면 UX 개선**
- 선수카드 목록: 닉네임순/성명순 토글 버튼 + 오름차순 정렬
- 신페리오 결과: 순위(#) 컬럼 추가, 1/2/3위 금/은/동 색상
- 리그순위 3차 기준 추가 (최근대회 성적)

---

### Ver 1.1 — 오픈 후 요구사항 반영 (2026-05-21)

**결과 공개 시점 제어 (완료구분 3단계)**
- `완료구분` 값 추가: `진행중` → `마감` → `완료` 3단계 상태 머신
- 진행중: 사용자 화면 대회 선택 콤보박스에 미표시
- 마감: 콤보박스 표시, 스트로크만 공개, 신페리오/번외타이틀/청백전 🔒 잠금
- 완료: 모든 탭 전체 공개

**관리자 4단계 → 결과조회 개편**
- 탭명 변경: "대회종료" → "결과조회"
- 상단 🚩 마감 버튼 / 하단 🏆 결과공개 버튼 분리
- 중단 서브탭 3개: 신페리오 / 번외타이틀 / 청백전

**선수카드 홀별 기록 개선**
- 구장명·코스명 헤더 추가
- 파 기호 행 추가 (파3=`•` 파란, 파4=공백, 파5=`—` 주황)

---

### Ver 1.0 — 그랜드 오픈 (2026-05-20)

**아키텍처 완전 전환**
- Google Sheets + GAS 백엔드 완전 폐기
- Supabase (PostgreSQL) 직접 연동으로 전환
- 응답 속도 10배 이상 개선 (GAS cold-start 제거)

**DB 스키마 정비**
- `tb_member`: PK = `전화번호`, `활동상태` 컬럼 추가
- `tb_Course`: PK = `(구장명, 코스명)` 복합키, `사용여부` 컬럼 추가
- `tb_Record`: PK = `(대회번호, 전화번호)` 복합키
- `tb_Season`: `종료_대회번호` bigint 타입으로 정규화 (NULL = 진행중)

**관리자 화면 전면 개편**
- 4개 메인 탭: 대회관리 / 회원관리 / 코스관리 / 시즌관리
- 대회관리 4단계 구조 (세팅 → 매핑 → 스코어 → 종료)
- 회원 등록: 성별 선택 + 자동채번 (010-1000/2000-XXXX)

**신페리오 엔진 고도화**
- 계산 로직 GAS(서버)에서 브라우저 JavaScript로 이전
- NP_Handicap, Net_Score, Luck_Factor 소수 둘째자리 정밀도

---

### BETA v1.2 (구 버전 — 폐기)
- GitHub + Vercel 프론트엔드 분리, GAS 병행 하이브리드 아키텍처
- PWA 앱 설치 지원 (manifest.json / manifest-admin.json)

### BETA v1.1 (구 버전 — 폐기)
- 번외타이틀 통합 테이블, 청백팀 결과, 선수카드 그래프

### BETA v1.0 (구 버전 — 폐기)
- 시즌제 승강제, 신페리오 정산, AI JSON 입력 초기 구현 (Google Sheets 기반)
