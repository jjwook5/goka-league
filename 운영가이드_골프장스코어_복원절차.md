# 🏌️ 골프장 제공 스코어 복원 절차 (자유CC 등)

> 관리자가 직접 라운드에 참석하지 않아 **골프장 시스템 스코어 + 골프장 신페리오 결과**로
> 대회를 정산해야 할 때 따르는 절차. (스마트스코어 미사용 케이스)
> 최초 정리: 2026-06-12 월례회(자유CC) 복원 경험 기반.

---

## 0. 핵심 전제

- **골프장 스코어카드는 항상 코스 순서(OUT 먼저)로 출력된다.**
  선수가 실제로 IN에서 출발했어도 카드는 OUT 9홀이 먼저, IN 9홀이 나중.
- **신페리오 점수(Net)는 골프장이 제공한 값을 그대로 신뢰**한다. 앱이 재계산하지 않는다.
- 우리 시스템 `tb_Record`는 물리적으로 H1~H18 컬럼만 있고,
  "어느 코스 홀인지"는 `전반코스`/`후반코스` 라벨이 알려준다.

---

## 1. 2단계 현장매핑 — 전 조 `OUT-IN`으로 통일 ★

- 코스 우선순위는 골프장 사정(공사 등)으로 가변적이라 자동화하지 않는다.
- **모든 조를 전반=OUT, 후반=IN으로 등록**한다. (카드 출력 순서와 일치)
- 이렇게 하면 카드 JSON을 swap 없이 그대로 넣어도 H1~9=OUT, H10~18=IN로 일관.

**트레이드오프(감수):** IN 출발 조의 선수카드 홀별 표시가 코스 순서(OUT→IN)로 나오고
백카운트도 코스 기준 후반(IN)으로 잡힌다. 단 신페 점수는 골프장값이고
의미 있는 1등·꼴찌는 단독 점수라 실질 영향 없음.

---

## 2. 3단계 스코어 입력 — 카드 그대로

- AI로 카드 이미지 → JSON 추출(H1~H18, OUT 먼저) → 검증 → 💾 임시저장.
- 임시저장 시 `합계`(그로스) 자동 계산됨.
- **swap 불필요** (2단계를 OUT-IN으로 통일했으므로).

---

## 3. 신페리오 정산값 입력 — SQL로 역산

골프장이 준 **Net_Score**만 있으면 NP·Luck은 SQL로 역산된다 (제외홀 몰라도 됨).

```
NP_Handicap = 합계 − Net_Score              (항등식, 100% 정확)
Luck_Factor = NP_Handicap − (합계 − 기준타) × 0.8
기준타 = 72  (자유CC OUT 36 + IN 36)
```

```sql
update "tb_Record" as r set
  "Net_Score"   = v.net,
  "NP_Handicap" = round((r."합계" - v.net)::numeric, 2),
  "Luck_Factor" = round(((r."합계" - v.net) - (r."합계" - 72) * 0.8)::numeric, 2)
from (values
  ('성명1', 70.0), ('성명2', 70.6) /* ... 전원 */
) as v(name, net)
where r."성명" = v.name and r."대회번호" = <대회번호>;
```

- 실행 후 **영향 행 수 = 참가자 수**인지 확인 (성명 오타 시 매칭 누락).
- 검산: `합계 − NP_Handicap = Net_Score` 전원 일치.

---

## 4. (선택) 제외홀 역산 — 화면엔 영향 없음, 기록용

제외홀은 신페리오 순위·점수에 영향 없으므로 **나중에 채워도 된다.**

### 역산 방법 (더블페리아 원공식)

- 단순 역산 `excl6 = 18홀오버 − NP/1.2`는 **제외홀이 파별 균등일 때만** 성립.
  → 실제로는 파별 비균등일 수 있으므로 **원공식으로 풀어야 함:**

```
HDCP = (선택12홀 그로스 합 × 1.5 − 72) × 0.8
정수식:  HDCP×10 == 12 × (선택12홀그로스) − 576
```

- 전 참가자 데이터(H1~18을 OUT 먼저로 정규화)로 C(18,6) 전수 탐색,
  **모든 참가자의 HDCP를 동시에 만족하는 6홀 조합**을 찾는다.
- 참가자가 많을수록(20명+) 유일해로 수렴. swap 방향도 0개 vs 1개로 검증됨.

### 2026-06-12 자유CC 실제 결과 (참고)

- 코스 파 — OUT: `4,4,3,4,5,4,3,5,4` / IN: `4,5,4,3,4,5,3,4,4`
- 제외홀(24명 유일해): **OUT 3·6·9번, IN 4·6·9번**
  - 카드순(H) 기준: `3,6,9,13,15,18`
  - OUT은 파3·파4·파4(파별 비균등!), IN은 파3·파4·파5(균등)
- 기재:
  ```sql
  update "tb_Match_Episodes" set "제외홀"='3,6,9' where "대회번호"=<번호> and "코스"='OUT';
  update "tb_Match_Episodes" set "제외홀"='4,6,9' where "대회번호"=<번호> and "코스"='IN';
  ```

### ⚠️ 절대 주의

- 제외홀이 **파별 비균등**이면 앱 신페리오 공식(`overParSelected×1.2`)과
  골프장 공식이 **다른 값**을 낸다.
- **관리자 화면 "🎲 제외홀 추첨 & 신페리오 정산" 버튼을 누르지 말 것.**
  누르면 직접 입력한 골프장 NP/Net/Luck을 덮어써서 순위가 틀어진다.

---

## 5. 공개

```sql
update "tb_Match_Episodes" set "완료구분"='완료' where "대회번호"=<번호>;
```
또는 관리자 4단계 🏆 결과공개 버튼. 공개 전 4개 탭 눈으로 확인.

---

## 요약 체크리스트

- [ ] 2단계: 전 조 OUT-IN 등록
- [ ] 3단계: 카드 그대로 입력 → 합계 자동
- [ ] Net_Score SQL로 NP·Luck 역산 입력 (행 수 = 인원 확인)
- [ ] (선택) 제외홀 원공식 역산 → 기재 / 단 정산버튼 금지
- [ ] 완료구분='완료' 공개
