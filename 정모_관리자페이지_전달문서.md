# 🏆 정모 팀 대항전 — 관리자 페이지 개발 전달문서

> 제1회 고카 정모(2026-09-13) 팀 대항전 기능의 현황과, 다음 세션에서 만들 **정모용 관리자 페이지** 요구사항.
> 회원용 정모 페이지(`event.html`)와 데이터는 이미 완성/생성됨. 남은 건 **관리자 입력 도구**.

> ✅ **완료 (2026-09-15) — 이 문서는 착수 시점 기록으로 보존.**
> 관리자 도구는 `admin/event.html`(수동 JSON) → **`admin/event2.html`(사진 AI 판독)** 로 완성되어
> 제1회 고카 정모(2026-09-13)에서 실제 운영했다. 이후 규칙 2건이 바뀌었다:
> **팀 동점은 각 팀 1위부터 비교**, **스트로크 우승자는 신페 우승에서 제외(👑 표시)**.
> 최신 사양은 `CLAUDE.md` · `GokaLeague_System_Specification.md`(Ver 1.4) 기준.

---

## 1. 정모 개요

- **대회번호**: `20260913` / **대회명**: 제1회 고카 정모 / **구장**: 파크벨리(파크 9홀 + 벨리 9홀, par 72)
- **36명 / 9조 / 4팀** — 조편성·팀배정·코스매핑까지 DB에 생성 완료(진행중 상태, 스코어는 비어있음)
- **팀**: 화이트(A) / 네이비(B) / 레드(C) / 블랙(D) — 각 9명, 각 조에 4팀 1명씩
- **코스 배정**: 파크 조 = 전반 파크·후반 벨리 / 벨리 조 = 전반 벨리·후반 파크

## 2. 완성된 것 (배포 완료)

| 파일 | 내용 |
|------|------|
| `event.html` | **회원용 정모 페이지** (라운드 중 / 최종 종합 / 개인시상 3탭). golfriend.vercel.app/event.html |
| `index.html` | 헤더 우측 **🏆 정모 바로가기 버튼** 추가됨 |

**event.html 3탭 로직 (모두 자동 계산, tb_Record 읽음):**
- **라운드 중**: 팀 합산 오버파(합계−72) 순위, 팀 클릭 시 선수별 오버파. `합계>0`인 선수만 집계.
- **최종 종합**: `완료구분='완료'`일 때만 공개. 순위점수 + 보너스 종합.
- **개인시상**: 신페리오/스트로크/번외타이틀 전원 순위 (기존 대회결과 스타일).

## 3. 점수 산정 방식 (사진 기준, event.html에 구현됨)

팀 총점 = 순위점수 + 보너스/페널티. 대부분 자동, 니어만 수동.

| 항목 | 점수 | 계산 | 자동/수동 |
|------|------|------|-----------|
| 팀 합산 스트로크 순위 | 1등100/2등90/3등80/4등70 | 팀 그로스 합 순위 | 자동 |
| 메달리스트 | +10 | 전체 최저 그로스 1명 | 자동 |
| 신페 남우승 | +10 | 남(1000번대) 중 Net 최저 | 자동 |
| 신페 여우승 | +10 | 여(2000번대) 중 Net 최저 | 자동 |
| 니어리스트 | +10 | 파3 지정홀 근접 | **수동** |
| 버디 1개당 | +5 | H=−1 개수(팀 합산) | 자동 |
| 신페 꼴등 | −10 | 전체 Net 최고 1명 | 자동 |

## 4. 데이터 구조

- **tb_Record**(대회번호=20260913): 소속팀(화이트/네이비/레드/블랙), 조번호(파크1조~벨리5조), 전반/후반코스, H1~18, 합계, NP_Handicap, Net_Score, Luck_Factor
- **tb_Match_Episodes**(20260913): 코스=파크/벨리 2행, 제외홀, 완료구분, **니어리스트**(text, 전화번호 쉼표구분 — 컬럼 미생성, 니어 입력 시 `alter table`로 추가)
- 성별 판별: 전화번호 번호대(1000=남, 2000=여)

## 5. ★ 관리자 페이지 요구사항 (다음 세션에서 만들 것)

정모 스코어는 **2단계로 입력**된다 (스마트스코어 앱 활용):

### (A) 라운드 중 — 실시간 스트로크
- 스마트스코어 **리더보드 캡처** → AI가 JSON(`{이름, 오버파, 진행홀}`) 추출 → `tb_Record.합계 = 72+오버파` 갱신
- 홀별(H1~18) 정보 없음 → 스트로크 순위만. 신페/버디는 이 단계에서 계산 불가.
- 조가 들어올 때마다 짬짬이 업로드 → 라운드 중 탭이 갱신됨

### (B) 라운드 후 — 최종 정산
- **스코어카드 캡처** → AI가 JSON(H1~18 오버파) 추출 → tb_Record H1~18 + 합계
- 신페리오 제외홀 추첨 + 정산(NP/Net/Luck) → 관리자 정산 실행
- 니어리스트 입력 → tb_Match_Episodes.니어리스트
- 완료구분='완료' → 최종 종합·개인시상 공개

### 필요 기능 (구현 대상)
1. **AI 사진 추출 API** — Vercel Serverless Function(`/api/extract-score.js`) + `ANTHROPIC_API_KEY`(Vercel 환경변수). 리더보드/스코어카드 두 프롬프트. ※ 사용자가 API 키 발급·크레딧 충전 필요(개인 구독과 별개, 종량제, 월 수백원 수준)
2. **정모 전용 관리 화면** — 리더보드 업로드(A) / 스코어카드 업로드(B) / 니어리스트 입력 / 완료 처리. 별도 페이지(예: `admin/event.html`) 권장 (기존 admin 무수정)
3. **니어리스트 입력 UI** — 파3 지정홀 니어 딴 선수 선택 → tb_Match_Episodes.니어리스트 저장

> 참고: 앞서 논의에서 "라운드 중 완전 실시간(홀별)은 비현실 → 리더보드 캡처로 준실시간"으로 합의. 스마트스코어 리더보드는 홀별이 없어 스트로크만 실시간, 나머지는 최종 정산에서.

## 6. 운영 워크플로우 (정모 당일)

1. 라운드 중: 리더보드 캡처 → AI → 합계 갱신 (스트로크 순위 중계)
2. 라운드 후: 스코어카드 캡처 → AI → H1~18 → 신페 정산
3. 니어리스트 입력
4. 완료구분='완료' → 최종 종합·개인시상 공개

## 7. 참고 SQL

**초기화(테스트 후 원복 — 이미 실행됨):**
```sql
update "tb_Record" set "H1"=null,...,"H18"=null,"합계"=0,"NP_Handicap"=0,"Net_Score"=0,"Luck_Factor"=0 where "대회번호"=20260913;
update "tb_Match_Episodes" set "제외홀"='', "완료구분"='진행중' where "대회번호"=20260913;
```

**니어리스트 입력:**
```sql
alter table "tb_Match_Episodes" add column "니어리스트" text;  -- 최초 1회
update "tb_Match_Episodes" set "니어리스트"='010-1000-0001,010-2000-0004' where "대회번호"=20260913 and "코스"='파크';
```

**완료 처리:**
```sql
update "tb_Match_Episodes" set "완료구분"='완료' where "대회번호"=20260913;
```

---

## 참고: 프로젝트 공통

- 순수 정적 HTML + Supabase REST(백엔드 없음).
  ⚠️ 작성 당시 표기(회원용=ANON_KEY, 관리자=SERVICE_ROLE_KEY)는 **폐기됨**.
  2026-09-11부터 **공개(publishable) 키 + 관리자 로그인 토큰(RLS)** 방식이다. service_role 키는 어디에도 두지 않는다.
- 배포: git push → Vercel 자동(~30초). 한글 컬럼명 URL은 encodeURIComponent 필수.
- 상세 명세: `GokaLeague_System_Specification.md`, 브리핑: `CLAUDE.md`
